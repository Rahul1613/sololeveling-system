import React from 'react';
import Quests from './Quests';

/**
 * QuestBoard component - Main entry point for the quest system
 * Redirects to the Quests component for a complete quest experience
 */
const QuestBoard = () => {
  return <Quests />;
};

export default QuestBoard;
